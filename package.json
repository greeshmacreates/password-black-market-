{
  "name": "password-black-market",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm start --prefix frontend\" \"npm run dev --prefix backend\"",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "setup": "cd backend && node setupFirebase.js",
    "admin": "node backend/add-admin.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}